/**
 * Shared config schema (zod) and default values used by web, desktop, and native shells.
 * Wire into apps as you formalize VITE_ / tauri::App env.
 */
export const appId = "com.goru.zecvault" as const;
