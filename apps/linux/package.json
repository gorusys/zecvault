{
  "name": "@zecvault/linux",
  "version": "0.1.9",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "tauri:build": ". \"$HOME/.cargo/env\" && export PATH=\"../../tools/bin:../../node_modules/.bin:$PATH\" && CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER=\"$(pwd)/../../tools/bin/cc\" CI=true tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1"
  }
}
